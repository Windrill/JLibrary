/**
 *
 * Tests
 *
 * let tester;
 * tester[x];
 * tester[y]
 * tester.add(x,3)
 * tester.sort([x,y,z])
 * looks like i dont need an array after all???! just sorting the map.
 * like a sorted map. with indices.
 */
class Hybrid extends Object {
  // it's an array and object

  constructor() {
    super();
  }
  // i want to push, splice.....add

  //

  enableKey() {}
  enableSort() {
    // ok now it's sortable
  }
}

export {
  Hybrid
}