/*import {Pair} from "../structures";
import {DrawSettings} from "../canvas";


class Direction {

  public x: number;
  public y: number;
  public z: number;

  constructor(a : number,b : number,c: number) {
    this.x = a;
    this.y = b;
    this.z = c;
  }
}


let vectorToDirection = (vector: Direction) => {
  // okay, just
// need to assume a forward vector???


  let ret : Direction = {
   x: 1, y: 2, z: 3
  };
  return ret;



}*/

export {}